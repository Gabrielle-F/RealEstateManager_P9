<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/bottom_bar_properties_list"
        android:title="@string/properties_list_bottom_bar"
        android:icon="@drawable/ic_properties_list"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/bottom_bar_add_property"
        android:title="@string/add_property_bottom_bar"
        android:icon="@drawable/ic_add_property_white"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/bottom_bar_currency_exchange"
        android:title="@string/currency_money_exchange_bottom_bar"
        android:icon="@drawable/ic_currency_exchange"
        app:showAsAction="ifRoom"
        />

</menu>